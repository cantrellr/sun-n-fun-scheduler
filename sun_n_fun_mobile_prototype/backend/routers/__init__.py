"""Router package initialization.

Exposing the routers as a package simplifies import paths in the `main.py`
application.  New routers (e.g. for audit events) should be added here and
imported in `backend/main.py`.
"""